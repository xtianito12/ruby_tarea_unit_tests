<%= form_with(model: farm, local: true) do |form| %>
  <% if farm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(farm.errors.count, "error") %> prohibited this farm from being saved:</h2>

      <ul>
      <% farm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :farm_name %>
  </div>

  <div class="field">
    <%= form.label :bought_on %>
    <%= form.datetime_select :bought_on, id: :farm_bought_on %>
  </div>

  <div class="field">
    <%= form.label :grower_id %>
    <%= form.text_field :grower_id, id: :farm_grower_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
